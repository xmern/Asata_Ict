import { FilenameDPipe } from './filename-d.pipe';

describe('FilenameDPipe', () => {
  it('create an instance', () => {
    const pipe = new FilenameDPipe();
    expect(pipe).toBeTruthy();
  });
});
